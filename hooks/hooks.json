{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/install.sh\""
          }
        ]
      },
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "CLI=\"${CLAUDE_PLUGIN_DATA}/build/packages/server/dist/cli.js\"; [ -f \"$CLI\" ] && node \"$CLI\" status 2>/dev/null | grep -qE '^[[:space:]]*\\[\\*\\]' && echo '[tot-mcp] An open reasoning session exists for this project. The tot-inspect skill views it; tot-reason continues it.' || true"
          }
        ]
      }
    ]
  }
}
