{
  "name": "tot-mcp",
  "version": "0.1.0",
  "description": "Tree of Thoughts MCP Server — structured hypothesis reasoning grounded in falsificationism and eliminative induction, with real-time visualization",
  "type": "module",
  "author": "Derek Kinzo",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/derekkinzo/tot.git",
    "directory": "packages/server"
  },
  "homepage": "https://github.com/derekkinzo/tot#readme",
  "engines": {
    "node": ">=20.11.0"
  },
  "bin": {
    "tot-mcp": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/",
    "static/",
    "README.md"
  ],
  "scripts": {
    "build": "tsup && npm run copy-ui",
    "copy-ui": "test -f ../web-ui/dist/index.html || (echo 'web-ui/dist not built; run `npm run build` from the workspace root' >&2 && exit 1) && rm -rf static.new && cp -r ../web-ui/dist static.new && rm -rf static && mv static.new static",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "mcp",
    "tree-of-thoughts",
    "reasoning",
    "hypothesis",
    "falsification",
    "eliminative-induction",
    "structured-thinking"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@tot-mcp/shared": "*",
    "sirv": "^3.0.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0",
    "@types/uuid": "^10.0.0"
  }
}
